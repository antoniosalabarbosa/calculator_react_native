import { TextInput } from "react-native";
import { Input_Style } from "../styles/AppStyles";

const Input = ()=>{
    return(
        <TextInput style={Input_Style.calculatorInput} />
    );
};

export default Input;